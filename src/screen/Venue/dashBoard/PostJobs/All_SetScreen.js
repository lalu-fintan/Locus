import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const All_SetScreen = () => {
  return (
    <View>
      <Text>All_SetScreen</Text>
    </View>
  );
};

export default All_SetScreen;

const styles = StyleSheet.create({});
